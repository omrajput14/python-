# Find the resolution of an image (simulated for simplicity)
def jpeg_res(filename):
    print("Function to find resolution")
    return 1920, 1080

width, height = jpeg_res("dummy.jpg")
print(f"The resolution of the image is {width}x{height}")
